#ifndef FAST_RANDINT_H
#define FAST_RANDINT_H

// WARNING: the argument on the next line must be at least 1 and at most 52.
int fast_randint(int max);  // Returns a random integer 0..(max-1)

#endif // FAST_RANDINT_H
